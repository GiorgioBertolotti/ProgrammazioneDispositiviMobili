<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home" >

    <fragment
        android:id="@+id/nav_home"
        android:name="it.unimib.quakeapp.ui.home.HomeFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
        android:id="@+id/nav_all_quakes"
        android:name="it.unimib.quakeapp.EarthquakeList"
        android:label="@string/menu_earthquakes"
        tools:layout="@layout/fragment_earthquake_list">
    </fragment>
</navigation>